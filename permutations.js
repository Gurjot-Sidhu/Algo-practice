/* 
Given a array of different integers, 
return all possible permutations without duplicates
*/


function permute(array){
    for(var i = 0; i < array.length; i++){
        let remnum = array.pop(1)
        renum = array.length -1 + i
    }
}