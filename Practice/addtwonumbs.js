/* 
Given two non empty linked arrays,
return the value of rhe each position added up within another array.
*/

function addTwoNumbers(l1,l2){
    var newl = []
    for(var i = 0; i < l1.length; i++){
        newl[i] = li[i] + l2[i];
    }
    return newl
}