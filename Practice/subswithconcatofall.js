/* 
Given a string s and and an array of strings, 
return all starting indices of substrings in s that is a 
concatenation of each word.
*/

function findSubs(s,words){
    var answers = []
    for(var i = 0; i < words.length;i++){
        if(s.includes(words[i])){

        }
    }
}